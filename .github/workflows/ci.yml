# name: CI

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v2

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: "18.x" # Specify Node.js version 18.x

#       - name: Install dependencies
#         run: yarn install

#       - name: Build the project
#         run: yarn build

#       - name: Run tests
#         run: yarn test

#       - name: Start the application
#         run: yarn start

name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18.x"

      - name: Install dependencies
        run: yarn install

      - name: Build the project
        run: yarn build

      - name: Run tests
        env:
          NODE_ENV: test
          PORT: 3000
          MONGODB_URL: mongodb+srv://virakson444:NfT5ug3pATwLxQxQ@bootcamp-project.lwadf47.mongodb.net/auths
        run: yarn test

      - name: Start the application
        env:
          NODE_ENV: production
          PORT: 3000
          MONGODB_URL: mongodb+srv://virakson444:NfT5ug3pATwLxQxQ@bootcamp-project.lwadf47.mongodb.net/auths
        run: yarn start
